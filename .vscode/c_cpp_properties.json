{
    "version": 4,
    "configurations":
    [
        {
            "name": "Configuration",
            "compilerPath": "${env:ALICE_COMPILER_PATH}",
            "cStandard": "c23",
            "compileCommands": "${workspaceFolder}/compile_commands.json"
        }
    ]
}