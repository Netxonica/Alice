cmake_minimum_required(VERSION 4.4.1)
set(CMAKE_CXX_EXTENSIONS off)
set(CMAKE_EXPORT_COMPILE_COMMANDS on)
message(STATUS "Building on ${CMAKE_BUILD_TYPE} mode...")
if(CMAKE_SYSTEM_NAME STREQUAL "Darwin")
    if(ALICE_STACK)
        math(EXPR ALICE_STACK_ALIGNED "(${ALICE_STACK} / 16384 + 1) * 16384")
    endif()
    set(CMAKE_OSX_ARCHITECTURES arm64)
    project(Alice VERSION 0.0.1 LANGUAGES CXX OBJCXX)
else()
    if(ALICE_STACK)
        math(EXPR ALICE_STACK_ALIGNED "(${ALICE_STACK} / 4096 + 1) * 4096")
    endif()
    project(Alice VERSION 0.0.1 LANGUAGES CXX)
endif()
get_property(ALICE_MULTI GLOBAL PROPERTY GENERATOR_IS_MULTI_CONFIG)
if(MSVC)
    foreach(var CMAKE_CXX_FLAGS CMAKE_CXX_FLAGS_DEBUG CMAKE_CXX_FLAGS_RELEASE)
        string(REPLACE "/EHsc" "" ${var} "${${var}}")
    endforeach()
    if(ALICE_MULTI)
        set(CMAKE_MSVC_RUNTIME_LIBRARY "MultiThreaded$<$<CONFIG:Debug>:Debug>")
    elseif(CMAKE_BUILD_TYPE STREQUAL "Debug")
        set(CMAKE_MSVC_RUNTIME_LIBRARY "MultiThreadedDebug")
    else()
        set(CMAKE_MSVC_RUNTIME_LIBRARY "MultiThreaded")
    endif()
endif()
set(ALICE_CLEAN COMMAND ${CMAKE_COMMAND} -E create_symlink "${CMAKE_BINARY_DIR}/compile_commands.json" "${CMAKE_CURRENT_SOURCE_DIR}/compile_commands.json")
string(TOLOWER "${CMAKE_SYSTEM_PROCESSOR}" ARCH_LOWER)
if(CMAKE_SYSTEM_NAME STREQUAL "Darwin")
    list(APPEND ALICE_CLEAN COMMAND ${CMAKE_COMMAND} -E remove_directory "${CMAKE_BINARY_DIR}/Alice.app/Contents/Resources")
    find_library(foundation Foundation)
    if(NOT foundation)
        message(FATAL_ERROR "Foundation not found")
    endif()
    find_library(appkit AppKit)
    if(NOT appkit)
        message(FATAL_ERROR "AppKit not found")
    endif()
    find_library(quartzcore QuartzCore)
    if(NOT quartzcore)
        message(FATAL_ERROR "QuartzCore not found")
    endif()
    find_library(metal Metal)
    if(NOT metal)
        message(FATAL_ERROR "Metal not found")
    endif()
    set(ALICE_RESOURCES_FOLDER "${CMAKE_CURRENT_SOURCE_DIR}/Resources/macOS")
    set(ALICE_SHADERS_FOLDER "${CMAKE_CURRENT_SOURCE_DIR}/Shader/MSL")
    file(GLOB_RECURSE ALICE_MACOS_RESOURCES CONFIGURE_DEPENDS "${CMAKE_CURRENT_SOURCE_DIR}/Resources/macOS/*")
    file(GLOB_RECURSE ALICE_METAL_SHADERS CONFIGURE_DEPENDS "${CMAKE_CURRENT_SOURCE_DIR}/Shader/MSL/**.metal")
    file(GLOB_RECURSE ALICE_OBJCPP_SOURCES CONFIGURE_DEPENDS "${CMAKE_CURRENT_SOURCE_DIR}/Source/**.mm")
    foreach(ALICE_FILE ${ALICE_MACOS_RESOURCES})
        file(RELATIVE_PATH ALICE_RESOURCE "${ALICE_RESOURCES_FOLDER}" "${ALICE_FILE}")
        get_filename_component(ALICE_RESOURCE_FILE "${ALICE_RESOURCE}" DIRECTORY)
        set_source_files_properties("${ALICE_FILE}" PROPERTIES MACOSX_PACKAGE_LOCATION "Resources/${ALICE_RESOURCE_FILE}")
    endforeach()
    foreach(ALICE_FILE ${ALICE_METAL_SHADERS})
        file(RELATIVE_PATH ALICE_RESOURCE "${ALICE_SHADERS_FOLDER}" "${ALICE_FILE}")
        get_filename_component(ALICE_RESOURCE_FILE "${ALICE_RESOURCE}" DIRECTORY)
        set_source_files_properties("${ALICE_FILE}" PROPERTIES MACOSX_PACKAGE_LOCATION "Resources/Shaders/${ALICE_RESOURCE_FILE}")
    endforeach()
    add_executable(Alice MACOSX_BUNDLE ${ALICE_MACOS_RESOURCES} ${ALICE_METAL_SHADERS} ${ALICE_OBJCPP_SOURCES})
    set_target_properties(Alice PROPERTIES MACOSX_BUNDLE_INFO_PLIST "${CMAKE_CURRENT_SOURCE_DIR}/Info.plist")
    target_link_libraries(Alice PRIVATE ${foundation} ${appkit} ${quartzcore} ${metal})
    target_compile_definitions(Alice PRIVATE alice_darwin=)
elseif(CMAKE_SYSTEM_NAME STREQUAL "Windows")
    list(APPEND ALICE_CLEAN COMMAND ${CMAKE_COMMAND} -E remove_directory "${CMAKE_BINARY_DIR}/Resources")
    if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/Resource/Windows" AND IS_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}/Resource/Windows")
        list(APPEND ALICE_CLEAN COMMAND ${CMAKE_COMMAND} -E copy_directory "${CMAKE_CURRENT_SOURCE_DIR}/Resource/Windows" "${CMAKE_BINARY_DIR}/Resources")
    endif()
    if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/Shader/HLSL" AND IS_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}/Shader/HLSL")
        list(APPEND ALICE_CLEAN COMMAND ${CMAKE_COMMAND} -E make_directory "${CMAKE_BINARY_DIR}/Resources/Shaders")
        list(APPEND ALICE_CLEAN COMMAND ${CMAKE_COMMAND} -E copy_directory "${CMAKE_CURRENT_SOURCE_DIR}/Shader/HLSL" "${CMAKE_BINARY_DIR}/Resources/Shaders")
    endif()
    add_executable(Alice WIN32 "${CMAKE_CURRENT_SOURCE_DIR}/ThirdParty/Agility/src/d3dx12/d3dx12_property_format_table.cpp")
    target_include_directories(Alice PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/ThirdParty/Agility/include" "${CMAKE_CURRENT_SOURCE_DIR}/ThirdParty/Agility/include/d3dx12" "${CMAKE_CURRENT_SOURCE_DIR}/ThirdParty/DXC/include")
    if(ARCH_LOWER MATCHES "^(x86_64|amd64)$")
        list(APPEND ALICE_CLEAN COMMAND ${CMAKE_COMMAND} -E copy_if_different "${CMAKE_CURRENT_SOURCE_DIR}/ThirdParty/Agility/bin/x64/D3D12Core.dll" "${CMAKE_BINARY_DIR}")
        list(APPEND ALICE_CLEAN COMMAND ${CMAKE_COMMAND} -E copy_if_different "${CMAKE_CURRENT_SOURCE_DIR}/ThirdParty/Agility/bin/x64/d3d12SDKLayers.dll" "${CMAKE_BINARY_DIR}")
        list(APPEND ALICE_CLEAN COMMAND ${CMAKE_COMMAND} -E copy_if_different "${CMAKE_CURRENT_SOURCE_DIR}/ThirdParty/DXC/bin/x64/dxcompiler.dll" "${CMAKE_BINARY_DIR}")
        list(APPEND ALICE_CLEAN COMMAND ${CMAKE_COMMAND} -E copy_if_different "${CMAKE_CURRENT_SOURCE_DIR}/ThirdParty/DXC/bin/x64/dxil.dll" "${CMAKE_BINARY_DIR}")
        target_link_directories(Alice PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/ThirdParty/DXC/lib/x64")
    else()
        list(APPEND ALICE_CLEAN COMMAND ${CMAKE_COMMAND} -E copy_if_different "${CMAKE_CURRENT_SOURCE_DIR}/ThirdParty/Agility/bin/arm64/D3D12Core.dll" "${CMAKE_BINARY_DIR}")
        list(APPEND ALICE_CLEAN COMMAND ${CMAKE_COMMAND} -E copy_if_different "${CMAKE_CURRENT_SOURCE_DIR}/ThirdParty/Agility/bin/arm64/d3d12SDKLayers.dll" "${CMAKE_BINARY_DIR}")
        list(APPEND ALICE_CLEAN COMMAND ${CMAKE_COMMAND} -E copy_if_different "${CMAKE_CURRENT_SOURCE_DIR}/ThirdParty/DXC/bin/arm64/dxcompiler.dll" "${CMAKE_BINARY_DIR}")
        list(APPEND ALICE_CLEAN COMMAND ${CMAKE_COMMAND} -E copy_if_different "${CMAKE_CURRENT_SOURCE_DIR}/ThirdParty/DXC/bin/arm64/dxil.dll" "${CMAKE_BINARY_DIR}")
        target_link_directories(Alice PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/ThirdParty/DXC/lib/arm64")
    endif()
    target_link_libraries(Alice PRIVATE dxguid dxgi dxcompiler dxil d3d12)
    target_compile_definitions(Alice PRIVATE WIN32_LEAN_AND_MEAN= alice_windows=)
elseif(CMAKE_SYSTEM_NAME STREQUAL "Linux")
    set(VULKAN_PATH "${CMAKE_CURRENT_SOURCE_DIR}/ThirdParty/Vulkan")
    set(VULKAN_VERSION "1.4.350.1")
    if(NOT EXISTS "${VULKAN_PATH}/${VULKAN_VERSION}")
        include(FetchContent)
        FetchContent_Declare(VULKAN_DOWNLOAD URL "https://sdk.lunarg.com/sdk/download/${VULKAN_VERSION}/linux/vulkansdk-linux-x86_64-${VULKAN_VERSION}.tar.xz" SOURCE_DIR "${VULKAN_PATH}/${VULKAN_VERSION}")
        FetchContent_MakeAvailable(VULKAN_DOWNLOAD)
    endif()
    set(ENV{VULKAN_SDK} "${VULKAN_PATH}/${VULKAN_VERSION}/x86_64")
    find_package(Vulkan REQUIRED)
    find_package(PkgConfig REQUIRED)
    pkg_check_modules(WAYLAND REQUIRED IMPORTED_TARGET wayland-client)
    pkg_check_modules(WAYLAND_PROTOCOLS REQUIRED wayland-protocols)
    pkg_get_variable(WAYLAND_PROTOCOLS_DIR wayland-protocols pkgdatadir)
    find_program(WAYLAND_SCANNER wayland-scanner REQUIRED)
    set(WAYLAND_PROTO_OUT_DIR ${CMAKE_BINARY_DIR}/wayland-protocols)
    list(APPEND ALICE_CLEAN COMMAND ${CMAKE_COMMAND} -E make_directory "${WAYLAND_PROTO_OUT_DIR}")
    set(ALICE_WAYLAND_PROTOCOLS "")
    function(add_wayland_protocols)
        set(options)
        set(oneValueArgs)
        set(multiValueArgs XMLS)
        cmake_parse_arguments(ALICE_PROTO "${options}" "${oneValueArgs}" "${multiValueArgs}" ${ARGN})
        if(NOT ALICE_PROTO_XMLS)
            message(FATAL_ERROR "add_wayland_protocol: XMLS is required")
        endif()
        foreach(xml ${ALICE_PROTO_XMLS})
            set(PROTO_XML "${WAYLAND_PROTOCOLS_DIR}/stable/${xml}/${xml}.xml")
            set(GEN_H "${WAYLAND_PROTO_OUT_DIR}/${xml}-client-protocol.h")
            set(GEN_C "${WAYLAND_PROTO_OUT_DIR}/${xml}-client-protocol.c")
            list(APPEND ALICE_WAYLAND_PROTOCOLS ${GEN_C})
            set(ALICE_WAYLAND_PROTOCOLS "${ALICE_WAYLAND_PROTOCOLS}" PARENT_SCOPE)
            add_custom_command(OUTPUT ${GEN_H} ${GEN_C} COMMAND ${WAYLAND_SCANNER} client-header ${PROTO_XML} ${GEN_H} COMMAND ${WAYLAND_SCANNER} private-code ${PROTO_XML} ${GEN_C} DEPENDS ${PROTO_XML} VERBATIM)
        endforeach()
    endfunction()
    add_wayland_protocols(XMLS xdg-shell)
    list(APPEND ALICE_CLEAN COMMAND ${CMAKE_COMMAND} -E remove_directory "${CMAKE_BINARY_DIR}/Resources")
    if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/Resource/Linux" AND IS_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}/Resource/Linux")
        list(APPEND ALICE_CLEAN COMMAND ${CMAKE_COMMAND} -E copy_directory "${CMAKE_CURRENT_SOURCE_DIR}/Resource/Linux" "${CMAKE_BINARY_DIR}/Resources")
    endif()
    if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/Shader/GLSL" AND IS_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}/Shader/GLSL")
        list(APPEND ALICE_CLEAN COMMAND ${CMAKE_COMMAND} -E make_directory "${CMAKE_BINARY_DIR}/Resources/Shaders")
        list(APPEND ALICE_CLEAN COMMAND ${CMAKE_COMMAND} -E copy_directory "${CMAKE_CURRENT_SOURCE_DIR}/Shader/GLSL" "${CMAKE_BINARY_DIR}/Resources/Shaders")
    endif()
    add_executable(Alice)
    set(ALICE_VK_LAYER_PATH "${VULKAN_PATH}/${VULKAN_VERSION}/x86_64/share/vulkan/explicit_layer.d")
    target_compile_definitions(Alice PRIVATE ALICE_VK_PATH="${ALICE_VK_LAYER_PATH}")
    target_sources(Alice PRIVATE ${ALICE_WAYLAND_PROTOCOLS})
    target_include_directories(Alice PRIVATE ${WAYLAND_PROTO_OUT_DIR})
    target_link_libraries(Alice PRIVATE PkgConfig::WAYLAND Vulkan::Vulkan)
    target_compile_definitions(Alice PRIVATE alice_linux=)
else()
    message(FATAL_ERROR "Unsupported system: ${CMAKE_SYSTEM_NAME}. Alice currently supports Windows, Linux, and macOS.")
endif()
if(ARCH_LOWER MATCHES "^(x86_64|amd64)$")
    target_compile_definitions(Alice PRIVATE alice_amd64=)
    if(ALICE_SSE3)
        target_compile_definitions(Alice PRIVATE alice_sse3=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -msse3)
        endif()
    endif()
    if(ALICE_SSSE3)
        target_compile_definitions(Alice PRIVATE alice_ssse3=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -mssse3)
        endif()
    endif()
    if(ALICE_SSE4_1)
        target_compile_definitions(Alice PRIVATE alice_sse4_1=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -msse4.1)
        endif()
    endif()
    if(ALICE_SSE4_2)
        target_compile_definitions(Alice PRIVATE alice_sse4_2=)
        if(MSVC)
            target_compile_options(Alice PRIVATE /arch:SSE4.2)
        elseif(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -msse4.2)
        endif()
    endif()
    if(ALICE_SSE4A)
        target_compile_definitions(Alice PRIVATE alice_sse4a=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -msse4a)
        endif()
    endif()
    if(ALICE_AVX)
        target_compile_definitions(Alice PRIVATE alice_avx=)
        if(MSVC)
            target_compile_options(Alice PRIVATE /arch:AVX)
        elseif(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -mavx)
        endif()
    endif()
    if(ALICE_AVX2)
        target_compile_definitions(Alice PRIVATE alice_avx2=)
        if(MSVC)
            target_compile_options(Alice PRIVATE /arch:AVX2)
        elseif(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -mavx2)
        endif()
    endif()
    if(ALICE_AVX512)
        target_compile_definitions(Alice PRIVATE alice_avx512= alice_avx512f= alice_avx512cd= alice_avx512vl= alice_avx512bw= alice_avx512dq= alice_avx512ifma= alice_avx512vbmi= alice_avx512vpopcntdq= alice_avx512vp2intersect= alice_avx512vnni= alice_avx512vbmi2= alice_avx512bf16= alice_avx512fp16= alice_avx512bitalg= alice_avx512bmm=)
        if(MSVC)
            target_compile_options(Alice PRIVATE /arch:AVX512)
        elseif(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -mavx512f -mavx512cd -mavx512vl -mavx512bw -mavx512dq -mavx512ifma -mavx512vbmi -mavx512vpopcntdq -mavx512vp2intersect -mavx512vnni -mavx512vbmi2 -mavx512bf16 -mavx512fp16 -mavx512bitalg -mavx512bmm)
        endif()
    endif()
    if(ALICE_AVX512F)
        target_compile_definitions(Alice PRIVATE alice_avx512f=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -mavx512f)
        endif()
    endif()
    if(ALICE_AVX512CD)
        target_compile_definitions(Alice PRIVATE alice_avx512cd=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -mavx512cd)
        endif()
    endif()
    if(ALICE_AVX512VL)
        target_compile_definitions(Alice PRIVATE alice_avx512vl=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -mavx512vl)
        endif()
    endif()
    if(ALICE_AVX512BW)
        target_compile_definitions(Alice PRIVATE alice_avx512bw=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -mavx512bw)
        endif()
    endif()
    if(ALICE_AVX512DQ)
        target_compile_definitions(Alice PRIVATE alice_avx512dq=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -mavx512dq)
        endif()
    endif()
    if(ALICE_AVX512IFMA)
        target_compile_definitions(Alice PRIVATE alice_avx512ifma=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -mavx512ifma)
        endif()
    endif()
    if(ALICE_AVX512VBMI)
        target_compile_definitions(Alice PRIVATE alice_avx512vbmi=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -mavx512vbmi)
        endif()
    endif()
    if(ALICE_AVX512VPOPCNTDQ)
        target_compile_definitions(Alice PRIVATE alice_avx512vpopcntdq=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -mavx512vpopcntdq)
        endif()
    endif()
    if(ALICE_AVX512VP2INTERSECT)
        target_compile_definitions(Alice PRIVATE alice_avx512vp2intersect=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -mavx512vp2intersect)
        endif()
    endif()
    if(ALICE_AVX512VNNI)
        target_compile_definitions(Alice PRIVATE alice_avx512vnni=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -mavx512vnni)
        endif()
    endif()
    if(ALICE_AVX512VBMI2)
        target_compile_definitions(Alice PRIVATE alice_avx512vbmi2=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -mavx512vbmi2)
        endif()
    endif()
    if(ALICE_AVX512BF16)
        target_compile_definitions(Alice PRIVATE alice_avx512bf16=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -mavx512bf16)
        endif()
    endif()
    if(ALICE_AVX512FP16)
        target_compile_definitions(Alice PRIVATE alice_avx512fp16=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -mavx512fp16)
        endif()
    endif()
    if(ALICE_AVX512BITALG)
        target_compile_definitions(Alice PRIVATE alice_avx512bitalg=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -mavx512bitalg)
        endif()
    endif()
    if(ALICE_AVX512BMM)
        target_compile_definitions(Alice PRIVATE alice_avx512bmm=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -mavx512bmm)
        endif()
    endif()
    if(ALICE_AVXVNNI)
        target_compile_definitions(Alice PRIVATE alice_avxvnni=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -mavxvnni)
        endif()
    endif()
    if(ALICE_AVXIFMA)
        target_compile_definitions(Alice PRIVATE alice_avxifma=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -mavxifma)
        endif()
    endif()
    if(ALICE_AVXVNNIINT8)
        target_compile_definitions(Alice PRIVATE alice_avxvnniint8=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -mavxvnniint8)
        endif()
    endif()
    if(ALICE_AVXNECONVERT)
        target_compile_definitions(Alice PRIVATE alice_avxneconvert=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -mavxneconvert)
        endif()
    endif()
    if(ALICE_AVXVNNIINT16)
        target_compile_definitions(Alice PRIVATE alice_avxvnniint16=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -mavxvnniint16)
        endif()
    endif()
    if(ALICE_AVX10_1)
        target_compile_definitions(Alice PRIVATE alice_avx10_1=)
        if(MSVC)
            target_compile_options(Alice PRIVATE /arch:AVX10.1)
        elseif(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -mavx10.1)
        endif()
    endif()
    if(ALICE_AVX10_2)
        target_compile_definitions(Alice PRIVATE alice_avx10_2=)
        if(MSVC)
            target_compile_options(Alice PRIVATE /arch:AVX10.2)
        elseif(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -mavx10.2)
        endif()
    endif()
    if(ALICE_SHA)
        target_compile_definitions(Alice PRIVATE alice_sha=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -msha)
        endif()
    endif()
    if(ALICE_AES)
        target_compile_definitions(Alice PRIVATE alice_aes=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -maes)
        endif()
    endif()
    if(ALICE_PCLMUL)
        target_compile_definitions(Alice PRIVATE alice_pclmul=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -mpclmul)
        endif()
    endif()
    if(ALICE_CLFLUSHOPT)
        target_compile_definitions(Alice PRIVATE alice_clflushopt=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -mclflushopt)
        endif()
    endif()
    if(ALICE_CLWB)
        target_compile_definitions(Alice PRIVATE alice_clwb=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -mclwb)
        endif()
    endif()
    if(ALICE_FSGSBASE)
        target_compile_definitions(Alice PRIVATE alice_fsgsbase=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -mfsgsbase)
        endif()
    endif()
    if(ALICE_PTWRITE)
        target_compile_definitions(Alice PRIVATE alice_ptwrite=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -mptwrite)
        endif()
    endif()
    if(ALICE_RDRND)
        target_compile_definitions(Alice PRIVATE alice_rdrnd=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -mrdrnd)
        endif()
    endif()
    if(ALICE_F16C)
        target_compile_definitions(Alice PRIVATE alice_f16c=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -mf16c)
        endif()
    endif()
    if(ALICE_FMA)
        target_compile_definitions(Alice PRIVATE alice_fma=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -mfma)
        endif()
    endif()
    if(ALICE_FMA4)
        target_compile_definitions(Alice PRIVATE alice_fma4=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -mfma4)
        endif()
    endif()
    if(ALICE_PCONFIG)
        target_compile_definitions(Alice PRIVATE alice_pconfig=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -mpconfig)
        endif()
    endif()
    if(ALICE_WBNOINVD)
        target_compile_definitions(Alice PRIVATE alice_wbnoinvd=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -mwbnoinvd)
        endif()
    endif()
    if(ALICE_PREFETCHW)
        target_compile_definitions(Alice PRIVATE alice_prefetchw=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -mprfchw)
        endif()
    endif()
    if(ALICE_RDPID)
        target_compile_definitions(Alice PRIVATE alice_rdpid=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -mrdpid)
        endif()
    endif()
    if(ALICE_RDSEED)
        target_compile_definitions(Alice PRIVATE alice_rdseed=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -mrdseed)
        endif()
    endif()
    if(ALICE_SGX)
        target_compile_definitions(Alice PRIVATE alice_sgx=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -msgx)
        endif()
    endif()
    if(ALICE_XOP)
        target_compile_definitions(Alice PRIVATE alice_xop=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -mxop)
        endif()
    endif()
    if(ALICE_LWP)
        target_compile_definitions(Alice PRIVATE alice_lwp=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -mlwp)
        endif()
    endif()
    if(ALICE_POPCNT)
        target_compile_definitions(Alice PRIVATE alice_popcnt=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -mpopcnt)
        endif()
    endif()
    if(ALICE_ABM)
        target_compile_definitions(Alice PRIVATE alice_abm=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -mabm)
        endif()
    endif()
    if(ALICE_ADX)
        target_compile_definitions(Alice PRIVATE alice_adx=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -madx)
        endif()
    endif()
    if(ALICE_BMI)
        target_compile_definitions(Alice PRIVATE alice_bmi=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -mbmi)
        endif()
    endif()
    if(ALICE_BMI2)
        target_compile_definitions(Alice PRIVATE alice_bmi2=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -mbmi2)
        endif()
    endif()
    if(ALICE_LZCNT)
        target_compile_definitions(Alice PRIVATE alice_lzcnt=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -mlzcnt)
        endif()
    endif()
    if(ALICE_FXSR)
        target_compile_definitions(Alice PRIVATE alice_fxsr=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -mfxsr)
        endif()
    endif()
    if(ALICE_XSAVE)
        target_compile_definitions(Alice PRIVATE alice_xsave=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -mxsave)
        endif()
    endif()
    if(ALICE_XSAVEOPT)
        target_compile_definitions(Alice PRIVATE alice_xsaveopt=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -mxsaveopt)
        endif()
    endif()
    if(ALICE_XSAVEC)
        target_compile_definitions(Alice PRIVATE alice_xsavec=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -mxsavec)
        endif()
    endif()
    if(ALICE_XSAVES)
        target_compile_definitions(Alice PRIVATE alice_xsaves=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -mxsaves)
        endif()
    endif()
    if(ALICE_RTM)
        target_compile_definitions(Alice PRIVATE alice_rtm=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -mrtm)
        endif()
    endif()
    if(ALICE_HLE)
        target_compile_definitions(Alice PRIVATE alice_hle=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -mhle)
        endif()
    endif()
    if(ALICE_TBM)
        target_compile_definitions(Alice PRIVATE alice_tbm=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -mtbm)
        endif()
    endif()
    if(ALICE_MWAITX)
        target_compile_definitions(Alice PRIVATE alice_mwaitx=)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -mmwaitx)
        endif()
    endif()
    if(ALICE_APX)
        target_compile_definitions(Alice PRIVATE alice_apx=)
        if(MSVC)
            target_compile_options(Alice PRIVATE /feature:APX)
        endif()
    endif()
elseif(ARCH_LOWER MATCHES "^(aarch64|arm64)$")
    target_compile_definitions(Alice PRIVATE alice_arm64=)
    if(ALICE_ARM8_1)
        target_compile_definitions(Alice PRIVATE alice_arm8_1=)
        if(MSVC)
            target_compile_options(Alice PRIVATE /arch:armv8.1)
        endif()
    endif()
    if(ALICE_ARM8_2)
        target_compile_definitions(Alice PRIVATE alice_arm8_2=)
        if(MSVC)
            target_compile_options(Alice PRIVATE /arch:armv8.2)
        endif()
    endif()
    if(ALICE_ARM8_3)
        target_compile_definitions(Alice PRIVATE alice_arm8_3=)
        if(MSVC)
            target_compile_options(Alice PRIVATE /arch:armv8.3)
        endif()
    endif()
    if(ALICE_ARM8_4)
        target_compile_definitions(Alice PRIVATE alice_arm8_4=)
        if(MSVC)
            target_compile_options(Alice PRIVATE /arch:armv8.4)
        endif()
    endif()
    if(ALICE_ARM8_5)
        target_compile_definitions(Alice PRIVATE alice_arm8_5=)
        if(MSVC)
            target_compile_options(Alice PRIVATE /arch:armv8.5)
        endif()
    endif()
    if(ALICE_ARM8_6)
        target_compile_definitions(Alice PRIVATE alice_arm8_6=)
        if(MSVC)
            target_compile_options(Alice PRIVATE /arch:armv8.6)
        endif()
    endif()
    if(ALICE_ARM8_7)
        target_compile_definitions(Alice PRIVATE alice_arm8_7=)
        if(MSVC)
            target_compile_options(Alice PRIVATE /arch:armv8.7)
        endif()
    endif()
    if(ALICE_ARM8_8)
        target_compile_definitions(Alice PRIVATE alice_arm8_8=)
        if(MSVC)
            target_compile_options(Alice PRIVATE /arch:armv8.8)
        endif()
    endif()
    if(ALICE_ARM8_9)
        target_compile_definitions(Alice PRIVATE alice_arm8_9=)
        if(MSVC)
            target_compile_options(Alice PRIVATE /arch:armv8.9)
        endif()
    endif()
    if(ALICE_ARM9_0)
        target_compile_definitions(Alice PRIVATE alice_arm9_0=)
        if(MSVC)
            target_compile_options(Alice PRIVATE /arch:armv9.0)
        endif()
    endif()
    if(ALICE_ARM9_1)
        target_compile_definitions(Alice PRIVATE alice_arm9_1=)
        if(MSVC)
            target_compile_options(Alice PRIVATE /arch:armv9.1)
        endif()
    endif()
    if(ALICE_ARM9_2)
        target_compile_definitions(Alice PRIVATE alice_arm9_2=)
        if(MSVC)
            target_compile_options(Alice PRIVATE /arch:armv9.2)
        endif()
    endif()
    if(ALICE_ARM9_3)
        target_compile_definitions(Alice PRIVATE alice_arm9_3=)
        if(MSVC)
            target_compile_options(Alice PRIVATE /arch:armv9.3)
        endif()
    endif()
    if(ALICE_ARM9_4)
        target_compile_definitions(Alice PRIVATE alice_arm9_4=)
        if(MSVC)
            target_compile_options(Alice PRIVATE /arch:armv9.4)
        endif()
    endif()
else()
    message(FATAL_ERROR "Unknown architecture: ${CMAKE_SYSTEM_PROCESSOR}. Alice only recognizes amd64 and arm64")
endif()
target_compile_definitions(Alice PRIVATE alice_major=0 alice_middle=0 alice_minor=1)
target_include_directories(Alice PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/Source")
if(MSVC)
    target_compile_options(Alice PRIVATE /std:c++latest)
    if(ALICE_STACK)
        target_link_options(Alice PRIVATE "/F ${ALICE_STACK_ALIGNED}")
    endif()
else()
    target_compile_features(Alice PRIVATE cxx_std_26)
endif()
if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
    target_compile_options(Alice PRIVATE -Werror -Wall -Wextra -Wpedantic -Wsign-conversion -fno-rtti -fno-exceptions -fabi-version=21 -faligned-new -fchar8_t -fconcepts -fcontracts -fcontracts-client-check=all -fcontracts-definition-check=off -fcoroutines -ffold-simple-inlines -fno-gnu-keywords -fnew-inheriting-ctors -fnew-ttp-matching -frange-for-ext-temps -freflection -fsized-deallocation -fstrict-enums -fstrong-eval-order -fuse-cxa-atexit -Wabi-tag -Wcomma-subscript -Wdangling-reference -Wdelete-non-virtual-dtor -Wdeprecated-copy -Wdeprecated-copy-dtor -Wdeprecated-literal-operator -Wdeprecated-variadic-comma-omission -Winvalid-constexpr -Wnoexcept -Wnoexcept-type -Wclass-memaccess -Wnon-virtual-dtor -Wregister -Wreorder -Wrange-loop-construct -Wredundant-tags -Wstrict-null-sentinel -Wold-style-cast -Woverloaded-virtual=2 -Wsign-promo -Wmismatched-new-delete -Wmismatched-tags -Wvolatile -Waligned-new=all -Wplacement-new=2 -Wconditionally-supported -Wextra-semi -Wsized-deallocation -Wsuggest-final-types -Wsuggest-final-methods -Wsuggest-override -Wpsabi -Wchar-subscripts -Wdouble-promotion -Wformat=2 -Wformat-overflow=2 -Wformat-signedness -Wformat-truncation=2 -Wnonnull -Wnonnull-compare -Wnull-dereference -Wmaybe-musttail-local-addr -Wnrvo -Winfinite-recursion -Winit-self -Whardened -Wimplicit-fallthrough=5 -Wignored-qualifiers -Wmain -Wmisleading-indentation -Wmissing-attributes -Wmissing-braces -Wmissing-include-dirs -Wmismatched-dealloc -Wmultistatement-macros -Wparentheses -Wsequence-point -Wreturn-type -Wshift-negative-value -Wshift-overflow=2 -Wswitch -Wswitch-default -Wswitch-enum -Wsync-nand -Wtrivial-auto-var-init -Wunused-but-set-parameter -Wunused-but-set-variable -Wunused-function -Wunused-label -Wunused-local-typedefs -Wunused-parameter -Wunused-variable -Wunused-const-variable -Wunused-value -Wunused -Wuse-after-free=3 -Wunknown-pragmas -Wstrict-aliasing -Wstrict-overflow=1 -Wstring-compare -Wstringop-overflow -Wstrict-flex-arrays -Walloc-size -fstrict-flex-arrays -Walloc-zero -Wcalloc-transposed-args -Warith-conversion -Warray-bounds -Warray-compare -Warray-parameter -Wattribute-alias=2 -Wbidi-chars=any -Wbool-compare -Wbool-operation -Wduplicated-branches -Wduplicated-cond -Wframe-address -Wzero-as-null-pointer-constant -Wzero-length-bounds -Wtautological-compare -Wtrampolines -Wfree-nonheap-object -Wpointer-arith -Wtype-limits -Wcomments -Wtrigraphs -Wundef -Wexpansion-to-defined -Wcast-qual -Wcast-align -Wcast-function-type -Wcast-user-defined -Wwrite-strings -Wclobbered -Wconversion -Wdangling-else -Wdangling-pointer -Wempty-body -Wenum-compare -Wenum-conversion -Wsign-compare -Wsign-conversion -Wflex-array-member-not-at-end -Wfloat-conversion -Wsizeof-array-div -Wsizeof-pointer-div -Wsizeof-pointer-memaccess -Wmemset-elt-size -Wmemset-transposed-args -Waddress -Wlogical-op -Wlogical-not-parentheses -Wkeyword-macro -Wheader-guard -Wnormalized=nfc -Wpacked -Wpacked-not-aligned -Wredundant-decls -Wrestrict -Winterference-size -Wint-in-bool-context -Winvalid-utf8 -Wvla -Wvla-parameter -Wvolatile-register-var -pthread -fstack-reuse=all -freg-struct-return -fPIE -fno-plt)
    target_link_options(Alice PRIVATE -pthread -flink-libatomic -fuse-ld=wild -static-libgcc -static-libstdc++ -pie)
    if(CMAKE_SYSTEM_NAME STREQUAL "Windows")
        target_compile_definitions(Alice PRIVATE _UNICODE=)
        target_compile_options(Alice PRIVATE -municode)
    endif()
    if(ALICE_SAFE_BUFFERS)
        target_compile_options(Alice PRIVATE -fstack-protector-all)
    endif()
    if(ALICE_SAFE_CONTROL_FLOW)
        if(ALICE_SAFE_STACK)
            target_compile_options(Alice PRIVATE -fhardened)
        endif()
        target_compile_options(Alice PRIVATE -fcf-protection=full -fharden-compares -fharden-conditional-branches -fharden-control-flow-redundancy -fhardcfr-skip-leaf -fhardcfr-check-returning-calls -fhardcfr-check-noreturn-calls=always -fvtable-verify=std)
    endif()
    if(ALICE_SAFE_STACK)
        target_compile_options(Alice PRIVATE -fstack-check -fstack-clash-protection)
    endif()
elseif(CMAKE_CXX_COMPILER_ID STREQUAL "Clang")
    if(CMAKE_CXX_COMPILER_FRONTEND_VARIANT STREQUAL "GNU")
        target_compile_options(Alice PRIVATE -Werror -Wall -Wextra -Wpedantic -Wconversion -Wsign-conversion -fno-rtti -fno-exceptions -Wno-dollar-in-identifier-extension)
        if(CMAKE_SYSTEM_NAME STREQUAL "Windows")
            target_compile_definitions(Alice PRIVATE _UNICODE=)
            target_compile_options(Alice PRIVATE -municode)
        endif()
    else()
        message(FATAL_ERROR "Unsupported Clang's front-end variant: ${CMAKE_CXX_COMPILER_FRONTEND_VARIANT}. Alice currently supports GNU Clang.")
    endif()
elseif(MSVC)
    target_compile_definitions(Alice PRIVATE _HAS_EXCEPTIONS=0)
    if(ALICE_SAFE_BUFFERS)
        target_compile_options(Alice PRIVATE /GS)
    endif()
    if(ALICE_SAFE_CONTROL_FLOW)
        target_compile_options(Alice PRIVATE /guard:cf)
        target_link_options(Alice PRIVATE /guard:cf /CETCOMPAT)
    endif()
    if(ALICE_SAFE_STACK)
        target_compile_options(Alice PRIVATE /presetPadding)
    endif()
    if(ALICE_SPECTRE)
        target_compile_options(Alice PRIVATE /Qspectre /Qspectre-load /Qspectre-load-cf)
    endif()
    target_compile_options(Alice PRIVATE /nologo /bigobj /utf-8 /Zc:preprocessor /W4 /WX /permissive- /GR- /EHs-c- /GA /GF /Gw /Gy /jumptablerdata /options:strict /MP /volatile:iso /Zc:__cplusplus /Zc:enumTypes /Zc:templateScope /Zc:throwingNew)
    target_link_options(Alice PRIVATE /NOLOGO /OPT:REF,ICF,LBR /INCREMENTAL:NO /WX)
else()
    message(FATAL_ERROR "Unsupported C++ compiler: ${CMAKE_CXX_COMPILER_ID}. Alice currently supports GNU GCC, GNU Clang, or MSVC.")
endif()
if(ALICE_MULTI)
    target_compile_definitions(Alice PRIVATE $<$<CONFIG:Debug>:alice_debug=>)
    if(MSVC)
        if(ALICE_SAFE_STACK)
            target_compile_options(Alice PRIVATE $<$<CONFIG:Debug>:/RTC1>)
        endif()
        target_compile_options(Alice PRIVATE $<$<CONFIG:Debug>:/Zi /Od /Zf /ZH:SHA512> $<$<CONFIG:Release>:/GL /Ox /Oi>)
        target_link_options(Alice PRIVATE $<$<CONFIG:Debug>:/DEBUG> $<$<CONFIG:Release>:/LTCG /SPGO>)
    else()
        target_compile_options(Alice PRIVATE $<$<CONFIG:Debug>:-O0> $<$<CONFIG:Release>:-O3>)
        target_link_options(Alice PRIVATE $<$<CONFIG:Debug>:-g>)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            if(ALICE_SAFE_CONTROL_FLOW)
                target_compile_options(Alice PRIVATE $<$<CONFIG:Debug>:-fvtv-debug -fvtv-counts>)
            endif()
            if(ALICE_DEVELOPMENT)
                target_compile_options(Alice PRIVATE $<$<CONFIG:Release>:-fprofile-generate>)
                target_link_options(Alice PRIVATE $<$<CONFIG:Release>:-fprofile-generate>)
            else()
                target_compile_options(Alice PRIVATE $<$<CONFIG:Release>:-fprofile-use -fbranch-probabilities>)
                target_link_options(Alice PRIVATE $<$<CONFIG:Release>:-fprofile-use>)
            endif()
            target_compile_options(Alice PRIVATE $<$<CONFIG:Debug>:-g3 -ggdb3 -gdwarf-5 -femit-class-debug-always -fvar-tracking -fvar-tracking-assignments -fvar-tracking-uninit -gsplit-dwarf -gdwarf64 -gdescribe-dies -gpubnames -ggnu-pubnames -fdebug-types-section -grecord-gcc-switches -gas-loc-support -gas-locview-support -gcolumn-info -gstatement-frontiers -gvariable-location-views -ginline-points -gz -funwind-tables -fasynchronous-unwind-tables> $<$<CONFIG:Release>:-flto=auto -fforward-propagate -fomit-frame-pointer -foptimize-crc -foptimize-sibling-calls -foptimize-strlen -finline-atomics -finline-small-functions -findirect-inlining -finline-functions -finline-functions-called-once -fearly-inlining -fipa-sra -fmerge-constants -ffuse-ops-with-volatile-access -fthread-jumps -fsplit-wide-types -fsplit-wide-types-early -fcse-follow-jumps -fcse-skip-blocks -frerun-cse-after-loop -fgcse -fgcse-lm -fgcse-sm -fgcse-las -fgcse-after-reload -faggressive-loop-optimizations -fcrossjumping -fauto-inc-dec -fdce -fdse -fif-conversion -fif-conversion2 -fdeclone-ctor-dtor -fdelete-null-pointer-checks -fdevirtualize -fdevirtualize-speculatively -fdevirtualize-at-ltrans -fexpensive-optimizations -fext-dce -free -flive-range-shrinkage -fira-algorithm=CB -fira-region=all -fira-hoist-pressure -fira-loop-pressure -flra-remat -fschedule-insns -fschedule-insns2 -fsched-pressure -fsched-spec-load -fsched-stalled-insns=0 -fsched-stalled-insns-dep -fsched2-use-superblocks -fsched-group-heuristic -fsched-critical-path-heuristic -fsched-spec-insn-heuristic -fsched-rank-heuristic -fsched-last-insn-heuristic -fsched-dep-count-heuristic -fspeculatively-call-stored-functions -freschedule-modulo-scheduled-loops -fselective-scheduling -fselective-scheduling2 -fsel-sched-pipelining -fsel-sched-pipelining-outer-loops -fshrink-wrap -fshrink-wrap-separate -fcaller-saves -fcombine-stack-adjustments -fipa-ra -ftree-reassoc -fcode-hoisting -ftree-pre -ftree-partial-pre -ftree-forwprop -ftree-fre -ftree-phiprop -fhoist-adjacent-loads -ftree-copy-prop -fipa-pure-const -fipa-reference -fipa-reference-addressable -fipa-reorder-for-locality -fipa-stack-alignment -fipa-pta -fipa-profile -fipa-modref -fipa-cp -fipa-cp-clone -fipa-bit-cp -fipa-vrp -fipa-icf -flate-combine-instructions -fisolate-erroneous-paths-dereference -fisolate-erroneous-paths-attribute -ftree-sink -ftree-bit-ccp -ftree-ccp -fssa-backprop -fssa-phiopt -ftree-switch-conversion -ftree-tail-merge -ftree-cselim -ftree-dce -ftree-builtin-call-dce -ffinite-loops -ftree-dominator-opts -ftree-dse -ftree-ch -ftree-loop-optimize -ftree-loop-linear -floop-strip-mine -floop-block -floop-nest-optimize -ftree-coalesce-vars -ftree-loop-if-convert -ftree-loop-distribution -ftree-loop-distribute-patterns -floop-interchange -floop-unroll-and-jam -ftree-loop-im -ftree-loop-ivcanon -ftree-scev-cprop -fivopts -ftree-pta -ftree-sra -fstore-merging -ftree-ter -ftree-slsr -ftree-vectorize -fvect-cost-model=dynamic -ftree-vrp -fsplit-paths -fsplit-ivs-in-unroller -fpartial-inlining -fpredictive-commoning -freorder-blocks -freorder-blocks-algorithm=stc -freorder-blocks-and-partition -freorder-functions -fstrict-aliasing -fipa-strict-aliasing -falign-functions -flimit-function-alignment -falign-labels -falign-loops -falign-jumps -fweb -fuse-linker-plugin -fcompare-elim -ffold-mem-offsets -fcprop-registers -fprofile-correction -fprofile-partial-training -fauto-profile-inlining -fprofile-values -fprofile-reorder-functions -fvpt -frename-registers -fschedule-fusion -fdep-fusion -ftracer -funroll-loops -fpeel-loops -fmalloc-dce=2 -fmove-loop-invariants -fmove-loop-stores -fsplit-loops -funswitch-loops -fversion-loops-for-strides -ffunction-sections -fdata-sections -fstdarg-opt -fno-ident>)
            target_link_options(Alice PRIVATE $<$<CONFIG:Release>:-flto=auto LINKER:--gc-sections>)
        else()
            target_compile_options(Alice PRIVATE $<$<CONFIG:Debug>:-g> $<$<CONFIG:Release>:-flto=full>)
            target_link_options(Alice PRIVATE $<$<CONFIG:Release>:-flto=full>)
        endif()
    endif()
elseif(CMAKE_BUILD_TYPE STREQUAL "Debug")
    target_compile_definitions(Alice PRIVATE alice_debug=)
    if(MSVC)
        if(ALICE_SAFE_STACK)
            target_compile_options(Alice PRIVATE /RTC1)
        endif()
        target_compile_options(Alice PRIVATE /Zi /Od /Zf /ZH:SHA512)
        target_link_options(Alice PRIVATE /DEBUG)
    else()
        if(ALICE_SAFE_CONTROL_FLOW)
            target_compile_options(Alice PRIVATE -fvtv-debug -fvtv-counts)
        endif()
        target_compile_options(Alice PRIVATE -O0)
        target_link_options(Alice PRIVATE -g)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            target_compile_options(Alice PRIVATE -g3 -ggdb3 -gdwarf-5 -femit-class-debug-always -fvar-tracking -fvar-tracking-assignments -fvar-tracking-uninit -gsplit-dwarf -gdwarf64 -gdescribe-dies -gpubnames -ggnu-pubnames -fdebug-types-section -grecord-gcc-switches -gas-loc-support -gas-locview-support -gcolumn-info -gstatement-frontiers -gvariable-location-views -ginline-points -gz -funwind-tables -fasynchronous-unwind-tables)
        else()
            target_compile_options(Alice PRIVATE -g)
        endif()
    endif()
elseif(CMAKE_BUILD_TYPE STREQUAL "Release")
    if(MSVC)
        target_compile_options(Alice PRIVATE /GL /Ox /Oi)
        target_link_options(Alice PRIVATE /LTCG /SPGO)
    else()
        target_compile_options(Alice PRIVATE -O3)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            if(ALICE_DEVELOPMENT)
                target_compile_options(Alice PRIVATE -fprofile-generate)
                target_link_options(Alice PRIVATE -fprofile-generate)
            else()
                target_compile_options(Alice PRIVATE -fprofile-use -fbranch-probabilities)
                target_link_options(Alice PRIVATE -fprofile-use)
            endif()
            target_compile_options(Alice PRIVATE -flto=auto -fforward-propagate -fomit-frame-pointer -foptimize-crc -foptimize-sibling-calls -foptimize-strlen -finline-atomics -finline-small-functions -findirect-inlining -finline-functions -finline-functions-called-once -fearly-inlining -fipa-sra -fmerge-constants -ffuse-ops-with-volatile-access -fthread-jumps -fsplit-wide-types -fsplit-wide-types-early -fcse-follow-jumps -fcse-skip-blocks -frerun-cse-after-loop -fgcse -fgcse-lm -fgcse-sm -fgcse-las -fgcse-after-reload -faggressive-loop-optimizations -fcrossjumping -fauto-inc-dec -fdce -fdse -fif-conversion -fif-conversion2 -fdeclone-ctor-dtor -fdelete-null-pointer-checks -fdevirtualize -fdevirtualize-speculatively -fdevirtualize-at-ltrans -fexpensive-optimizations -fext-dce -free -flive-range-shrinkage -fira-algorithm=CB -fira-region=all -fira-hoist-pressure -fira-loop-pressure -flra-remat -fschedule-insns -fschedule-insns2 -fsched-pressure -fsched-spec-load -fsched-stalled-insns=0 -fsched-stalled-insns-dep -fsched2-use-superblocks -fsched-group-heuristic -fsched-critical-path-heuristic -fsched-spec-insn-heuristic -fsched-rank-heuristic -fsched-last-insn-heuristic -fsched-dep-count-heuristic -fspeculatively-call-stored-functions -freschedule-modulo-scheduled-loops -fselective-scheduling -fselective-scheduling2 -fsel-sched-pipelining -fsel-sched-pipelining-outer-loops -fshrink-wrap -fshrink-wrap-separate -fcaller-saves -fcombine-stack-adjustments -fipa-ra -ftree-reassoc -fcode-hoisting -ftree-pre -ftree-partial-pre -ftree-forwprop -ftree-fre -ftree-phiprop -fhoist-adjacent-loads -ftree-copy-prop -fipa-pure-const -fipa-reference -fipa-reference-addressable -fipa-reorder-for-locality -fipa-stack-alignment -fipa-pta -fipa-profile -fipa-modref -fipa-cp -fipa-cp-clone -fipa-bit-cp -fipa-vrp -fipa-icf -flate-combine-instructions -fisolate-erroneous-paths-dereference -fisolate-erroneous-paths-attribute -ftree-sink -ftree-bit-ccp -ftree-ccp -fssa-backprop -fssa-phiopt -ftree-switch-conversion -ftree-tail-merge -ftree-cselim -ftree-dce -ftree-builtin-call-dce -ffinite-loops -ftree-dominator-opts -ftree-dse -ftree-ch -ftree-loop-optimize -ftree-loop-linear -floop-strip-mine -floop-block -floop-nest-optimize -ftree-coalesce-vars -ftree-loop-if-convert -ftree-loop-distribution -ftree-loop-distribute-patterns -floop-interchange -floop-unroll-and-jam -ftree-loop-im -ftree-loop-ivcanon -ftree-scev-cprop -fivopts -ftree-pta -ftree-sra -fstore-merging -ftree-ter -ftree-slsr -ftree-vectorize -fvect-cost-model=dynamic -ftree-vrp -fsplit-paths -fsplit-ivs-in-unroller -fpartial-inlining -fpredictive-commoning -freorder-blocks -freorder-blocks-algorithm=stc -freorder-blocks-and-partition -freorder-functions -fstrict-aliasing -fipa-strict-aliasing -falign-functions -flimit-function-alignment -falign-labels -falign-loops -falign-jumps -fweb -fuse-linker-plugin -fcompare-elim -ffold-mem-offsets -fcprop-registers -fprofile-correction -fprofile-partial-training -fauto-profile-inlining -fprofile-values -fprofile-reorder-functions -fvpt -frename-registers -fschedule-fusion -fdep-fusion -ftracer -funroll-loops -fpeel-loops -fmalloc-dce=2 -fmove-loop-invariants -fmove-loop-stores -fsplit-loops -funswitch-loops -fversion-loops-for-strides -ffunction-sections -fdata-sections -fstdarg-opt -fno-ident)
            target_link_options(Alice PRIVATE -flto=auto LINKER:--gc-sections)
        else()
            target_compile_options(Alice PRIVATE -flto=full)
            target_link_options(Alice PRIVATE -flto=full)
        endif()
    endif()
else()
    message(FATAL_ERROR "Unsupported configuration. Alice currently supports Debug and Release.")
endif()
file(GLOB_RECURSE ALICE_EDITOR_SOURCES CONFIGURE_DEPENDS "${CMAKE_CURRENT_SOURCE_DIR}/Editor/*.cpp")
file(GLOB_RECURSE ALICE_SOURCES CONFIGURE_DEPENDS "${CMAKE_CURRENT_SOURCE_DIR}/Source/*.cpp")
target_sources(Alice PRIVATE ${ALICE_EDITOR_SOURCES} ${ALICE_SOURCES})
get_filename_component(ALICE_MAIN "${CMAKE_CURRENT_SOURCE_DIR}/Source/Alice.cpp" ABSOLUTE)
list(REMOVE_ITEM ALICE_SOURCES "${ALICE_MAIN}")
file(GLOB_RECURSE ALICE_TESTS CONFIGURE_DEPENDS "${CMAKE_CURRENT_SOURCE_DIR}/Test/*.cpp")
get_filename_component(ALICE_TEST_PATH "${CMAKE_CURRENT_SOURCE_DIR}/Test" ABSOLUTE)
set(ALICE_TESTING "")
enable_testing()
foreach(ALICE_TEST_FILE ${ALICE_TESTS})
    file(RELATIVE_PATH REL_PATH ${ALICE_TEST_PATH} "${ALICE_TEST_FILE}")
    string(REPLACE "/" "_" TARGET_NAME "${REL_PATH}")
    string(REPLACE "." "_" TARGET_NAME "${TARGET_NAME}")
    set(ALICE_TEST_NAME "ALICE_TEST_${TARGET_NAME}")
    if(CMAKE_SYSTEM_NAME STREQUAL "Darwin")
        list(APPEND ALICE_CLEAN COMMAND ${CMAKE_COMMAND} -E remove_directory "${CMAKE_BINARY_DIR}/${ALICE_TEST_NAME}.app/Contents/Resources")
        add_executable(${ALICE_TEST_NAME} MACOSX_BUNDLE ${ALICE_MACOS_RESOURCES} ${ALICE_METAL_SHADERS} ${ALICE_OBJCPP_SOURCES} ${ALICE_SOURCES} ${ALICE_TEST_FILE})
        set_target_properties(${ALICE_TEST_NAME} PROPERTIES MACOSX_BUNDLE_INFO_PLIST "${CMAKE_CURRENT_SOURCE_DIR}/Info.plist")
        target_link_libraries(${ALICE_TEST_NAME} PRIVATE ${foundation} ${appkit} ${quartzcore} ${metal})
        target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_darwin=)
    elseif(CMAKE_SYSTEM_NAME STREQUAL "Windows")
        add_executable(${ALICE_TEST_NAME} WIN32 ${ALICE_SOURCES} ${ALICE_TEST_FILE} "${CMAKE_CURRENT_SOURCE_DIR}/ThirdParty/Agility/src/d3dx12/d3dx12_property_format_table.cpp")
        target_include_directories(${ALICE_TEST_NAME} PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/ThirdParty/Agility/include" "${CMAKE_CURRENT_SOURCE_DIR}/ThirdParty/Agility/include/d3dx12" "${CMAKE_CURRENT_SOURCE_DIR}/ThirdParty/DXC/include")
        if(ARCH_LOWER MATCHES "^(x86_64|amd64)$")
            target_link_directories(${ALICE_TEST_NAME} PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/ThirdParty/DXC/lib/x64")
        else()
            target_link_directories(${ALICE_TEST_NAME} PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/ThirdParty/DXC/lib/arm64")
        endif()
        target_link_libraries(${ALICE_TEST_NAME} PRIVATE dxguid dxgi dxcompiler dxil d3d12)
        target_compile_definitions(${ALICE_TEST_NAME} PRIVATE WIN32_LEAN_AND_MEAN= alice_windows=)
    else()
        add_executable(${ALICE_TEST_NAME} ${ALICE_SOURCES} ${ALICE_TEST_FILE} ${ALICE_WAYLAND_PROTOCOLS})
        target_include_directories(${ALICE_TEST_NAME} PRIVATE ${WAYLAND_PROTO_OUT_DIR})
        target_link_libraries(${ALICE_TEST_NAME} PRIVATE PkgConfig::WAYLAND Vulkan::Vulkan)
        target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_linux=)
    endif()
    if(ARCH_LOWER MATCHES "^(x86_64|amd64)$")
        target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_amd64=)
        if(ALICE_SSE3)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_sse3=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -msse3)
            endif()
        endif()
        if(ALICE_SSSE3)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_ssse3=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -mssse3)
            endif()
        endif()
        if(ALICE_SSE4_1)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_sse4_1=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -msse4.1)
            endif()
        endif()
        if(ALICE_SSE4_2)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_sse4_2=)
            if(MSVC)
                target_compile_options(${ALICE_TEST_NAME} PRIVATE /arch:SSE4.2)
            elseif(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -msse4.2)
            endif()
        endif()
        if(ALICE_SSE4A)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_sse4a=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -msse4a)
            endif()
        endif()
        if(ALICE_AVX)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_avx=)
            if(MSVC)
                target_compile_options(${ALICE_TEST_NAME} PRIVATE /arch:AVX)
            elseif(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -mavx)
            endif()
        endif()
        if(ALICE_AVX2)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_avx2=)
            if(MSVC)
                target_compile_options(${ALICE_TEST_NAME} PRIVATE /arch:AVX2)
            elseif(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -mavx2)
            endif()
        endif()
        if(ALICE_AVX512)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_avx512= alice_avx512f= alice_avx512cd= alice_avx512vl= alice_avx512bw= alice_avx512dq= alice_avx512ifma= alice_avx512vbmi= alice_avx512vpopcntdq= alice_avx512vp2intersect= alice_avx512vnni= alice_avx512vbmi2= alice_avx512bf16= alice_avx512fp16= alice_avx512bitalg= alice_avx512bmm=)
            if(MSVC)
                target_compile_options(${ALICE_TEST_NAME} PRIVATE /arch:AVX512)
            elseif(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -mavx512f -mavx512cd -mavx512vl -mavx512bw -mavx512dq -mavx512ifma -mavx512vbmi -mavx512vpopcntdq -mavx512vp2intersect -mavx512vnni -mavx512vbmi2 -mavx512bf16 -mavx512fp16 -mavx512bitalg -mavx512bmm)
            endif()
        endif()
        if(ALICE_AVX512F)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_avx512f=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -mavx512f)
            endif()
        endif()
        if(ALICE_AVX512CD)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_avx512cd=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -mavx512cd)
            endif()
        endif()
        if(ALICE_AVX512VL)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_avx512vl=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -mavx512vl)
            endif()
        endif()
        if(ALICE_AVX512BW)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_avx512bw=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -mavx512bw)
            endif()
        endif()
        if(ALICE_AVX512DQ)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_avx512dq=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -mavx512dq)
            endif()
        endif()
        if(ALICE_AVX512IFMA)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_avx512ifma=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -mavx512ifma)
            endif()
        endif()
        if(ALICE_AVX512VBMI)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_avx512vbmi=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -mavx512vbmi)
            endif()
        endif()
        if(ALICE_AVX512VPOPCNTDQ)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_avx512vpopcntdq=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -mavx512vpopcntdq)
            endif()
        endif()
        if(ALICE_AVX512VP2INTERSECT)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_avx512vp2intersect=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -mavx512vp2intersect)
            endif()
        endif()
        if(ALICE_AVX512VNNI)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_avx512vnni=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -mavx512vnni)
            endif()
        endif()
        if(ALICE_AVX512VBMI2)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_avx512vbmi2=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -mavx512vbmi2)
            endif()
        endif()
        if(ALICE_AVX512BF16)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_avx512bf16=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -mavx512bf16)
            endif()
        endif()
        if(ALICE_AVX512FP16)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_avx512fp16=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -mavx512fp16)
            endif()
        endif()
        if(ALICE_AVX512BITALG)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_avx512bitalg=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -mavx512bitalg)
            endif()
        endif()
        if(ALICE_AVX512BMM)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_avx512bmm=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -mavx512bmm)
            endif()
        endif()
        if(ALICE_AVXVNNI)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_avxvnni=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -mavxvnni)
            endif()
        endif()
        if(ALICE_AVXIFMA)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_avxifma=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -mavxifma)
            endif()
        endif()
        if(ALICE_AVXVNNIINT8)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_avxvnniint8=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -mavxvnniint8)
            endif()
        endif()
        if(ALICE_AVXNECONVERT)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_avxneconvert=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -mavxneconvert)
            endif()
        endif()
        if(ALICE_AVXVNNIINT16)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_avxvnniint16=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -mavxvnniint16)
            endif()
        endif()
        if(ALICE_AVX10_1)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_avx10_1=)
            if(MSVC)
                target_compile_options(${ALICE_TEST_NAME} PRIVATE /arch:AVX10.1)
            elseif(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -mavx10.1)
            endif()
        endif()
        if(ALICE_AVX10_2)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_avx10_2=)
            if(MSVC)
                target_compile_options(${ALICE_TEST_NAME} PRIVATE /arch:AVX10.2)
            elseif(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -mavx10.2)
            endif()
        endif()
        if(ALICE_SHA)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_sha=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -msha)
            endif()
        endif()
        if(ALICE_AES)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_aes=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -maes)
            endif()
        endif()
        if(ALICE_PCLMUL)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_pclmul=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -mpclmul)
            endif()
        endif()
        if(ALICE_CLFLUSHOPT)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_clflushopt=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -mclflushopt)
            endif()
        endif()
        if(ALICE_CLWB)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_clwb=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -mclwb)
            endif()
        endif()
        if(ALICE_FSGSBASE)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_fsgsbase=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -mfsgsbase)
            endif()
        endif()
        if(ALICE_PTWRITE)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_ptwrite=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -mptwrite)
            endif()
        endif()
        if(ALICE_RDRND)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_rdrnd=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -mrdrnd)
            endif()
        endif()
        if(ALICE_F16C)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_f16c=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -mf16c)
            endif()
        endif()
        if(ALICE_FMA)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_fma=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -mfma)
            endif()
        endif()
        if(ALICE_FMA4)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_fma4=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -mfma4)
            endif()
        endif()
        if(ALICE_PCONFIG)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_pconfig=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -mpconfig)
            endif()
        endif()
        if(ALICE_WBNOINVD)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_wbnoinvd=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -mwbnoinvd)
            endif()
        endif()
        if(ALICE_PREFETCHW)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_prefetchw=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -mprfchw)
            endif()
        endif()
        if(ALICE_RDPID)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_rdpid=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -mrdpid)
            endif()
        endif()
        if(ALICE_RDSEED)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_rdseed=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -mrdseed)
            endif()
        endif()
        if(ALICE_SGX)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_sgx=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -msgx)
            endif()
        endif()
        if(ALICE_XOP)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_xop=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -mxop)
            endif()
        endif()
        if(ALICE_LWP)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_lwp=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -mlwp)
            endif()
        endif()
        if(ALICE_POPCNT)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_popcnt=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -mpopcnt)
            endif()
        endif()
        if(ALICE_ABM)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_abm=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -mabm)
            endif()
        endif()
        if(ALICE_ADX)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_adx=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -madx)
            endif()
        endif()
        if(ALICE_BMI)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_bmi=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -mbmi)
            endif()
        endif()
        if(ALICE_BMI2)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_bmi2=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -mbmi2)
            endif()
        endif()
        if(ALICE_LZCNT)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_lzcnt=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -mlzcnt)
            endif()
        endif()
        if(ALICE_FXSR)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_fxsr=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -mfxsr)
            endif()
        endif()
        if(ALICE_XSAVE)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_xsave=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -mxsave)
            endif()
        endif()
        if(ALICE_XSAVEOPT)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_xsaveopt=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -mxsaveopt)
            endif()
        endif()
        if(ALICE_XSAVEC)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_xsavec=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -mxsavec)
            endif()
        endif()
        if(ALICE_XSAVES)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_xsaves=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -mxsaves)
            endif()
        endif()
        if(ALICE_RTM)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_rtm=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -mrtm)
            endif()
        endif()
        if(ALICE_HLE)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_hle=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -mhle)
            endif()
        endif()
        if(ALICE_TBM)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_tbm=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -mtbm)
            endif()
        endif()
        if(ALICE_MWAITX)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_mwaitx=)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -mmwaitx)
            endif()
        endif()
        if(ALICE_APX)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_apx=)
            if(MSVC)
                target_compile_options(${ALICE_TEST_NAME} PRIVATE /feature:APX)
            endif()
        endif()
    elseif(ARCH_LOWER MATCHES "^(aarch64|arm64)$")
        target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_arm64=)
        if(ALICE_ARM8_1)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_arm8_1=)
            if(MSVC)
                target_compile_options(${ALICE_TEST_NAME} PRIVATE /arch:armv8.1)
            endif()
        endif()
        if(ALICE_ARM8_2)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_arm8_2=)
            if(MSVC)
                target_compile_options(${ALICE_TEST_NAME} PRIVATE /arch:armv8.2)
            endif()
        endif()
        if(ALICE_ARM8_3)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_arm8_3=)
            if(MSVC)
                target_compile_options(${ALICE_TEST_NAME} PRIVATE /arch:armv8.3)
            endif()
        endif()
        if(ALICE_ARM8_4)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_arm8_4=)
            if(MSVC)
                target_compile_options(${ALICE_TEST_NAME} PRIVATE /arch:armv8.4)
            endif()
        endif()
        if(ALICE_ARM8_5)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_arm8_5=)
            if(MSVC)
                target_compile_options(${ALICE_TEST_NAME} PRIVATE /arch:armv8.5)
            endif()
        endif()
        if(ALICE_ARM8_6)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_arm8_6=)
            if(MSVC)
                target_compile_options(${ALICE_TEST_NAME} PRIVATE /arch:armv8.6)
            endif()
        endif()
        if(ALICE_ARM8_7)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_arm8_7=)
            if(MSVC)
                target_compile_options(${ALICE_TEST_NAME} PRIVATE /arch:armv8.7)
            endif()
        endif()
        if(ALICE_ARM8_8)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_arm8_8=)
            if(MSVC)
                target_compile_options(${ALICE_TEST_NAME} PRIVATE /arch:armv8.8)
            endif()
        endif()
        if(ALICE_ARM8_9)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_arm8_9=)
            if(MSVC)
                target_compile_options(${ALICE_TEST_NAME} PRIVATE /arch:armv8.9)
            endif()
        endif()
        if(ALICE_ARM9_0)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_arm9_0=)
            if(MSVC)
                target_compile_options(${ALICE_TEST_NAME} PRIVATE /arch:armv9.0)
            endif()
        endif()
        if(ALICE_ARM9_1)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_arm9_1=)
            if(MSVC)
                target_compile_options(${ALICE_TEST_NAME} PRIVATE /arch:armv9.1)
            endif()
        endif()
        if(ALICE_ARM9_2)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_arm9_2=)
            if(MSVC)
                target_compile_options(${ALICE_TEST_NAME} PRIVATE /arch:armv9.2)
            endif()
        endif()
        if(ALICE_ARM9_3)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_arm9_3=)
            if(MSVC)
                target_compile_options(${ALICE_TEST_NAME} PRIVATE /arch:armv9.3)
            endif()
        endif()
        if(ALICE_ARM9_4)
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_arm9_4=)
            if(MSVC)
                target_compile_options(${ALICE_TEST_NAME} PRIVATE /arch:armv9.4)
            endif()
        endif()
    endif()
    target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_major=0 alice_middle=0 alice_minor=1)
    target_include_directories(${ALICE_TEST_NAME} PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/Source")
    if(MSVC)
        target_compile_options(${ALICE_TEST_NAME} PRIVATE /std:c++latest)
        if(ALICE_STACK)
            target_link_options(${ALICE_TEST_NAME} PRIVATE "/F ${ALICE_STACK_ALIGNED}")
        endif()
    else()
        target_compile_features(${ALICE_TEST_NAME} PRIVATE cxx_std_26)
    endif()
    if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
        target_compile_options(${ALICE_TEST_NAME} PRIVATE -Werror -Wall -Wextra -Wpedantic -Wsign-conversion -fno-rtti -fno-exceptions -fabi-version=21 -faligned-new -fchar8_t -fconcepts -fcontracts -fcontracts-client-check=all -fcontracts-definition-check=off -fcoroutines -ffold-simple-inlines -fno-gnu-keywords -fnew-inheriting-ctors -fnew-ttp-matching -frange-for-ext-temps -freflection -fsized-deallocation -fstrict-enums -fstrong-eval-order -fuse-cxa-atexit -Wabi-tag -Wcomma-subscript -Wdangling-reference -Wdelete-non-virtual-dtor -Wdeprecated-copy -Wdeprecated-copy-dtor -Wdeprecated-literal-operator -Wdeprecated-variadic-comma-omission -Winvalid-constexpr -Wnoexcept -Wnoexcept-type -Wclass-memaccess -Wnon-virtual-dtor -Wregister -Wreorder -Wrange-loop-construct -Wredundant-tags -Wstrict-null-sentinel -Wold-style-cast -Woverloaded-virtual=2 -Wsign-promo -Wmismatched-new-delete -Wmismatched-tags -Wvolatile -Waligned-new=all -Wplacement-new=2 -Wconditionally-supported -Wextra-semi -Wsized-deallocation -Wsuggest-final-types -Wsuggest-final-methods -Wsuggest-override -Wpsabi -Wchar-subscripts -Wdouble-promotion -Wformat=2 -Wformat-overflow=2 -Wformat-signedness -Wformat-truncation=2 -Wnonnull -Wnonnull-compare -Wnull-dereference -Wmaybe-musttail-local-addr -Wnrvo -Winfinite-recursion -Winit-self -Whardened -Wimplicit-fallthrough=5 -Wignored-qualifiers -Wmain -Wmisleading-indentation -Wmissing-attributes -Wmissing-braces -Wmissing-include-dirs -Wmismatched-dealloc -Wmultistatement-macros -Wparentheses -Wsequence-point -Wreturn-type -Wshift-negative-value -Wshift-overflow=2 -Wswitch -Wswitch-default -Wswitch-enum -Wsync-nand -Wtrivial-auto-var-init -Wunused-but-set-parameter -Wunused-but-set-variable -Wunused-function -Wunused-label -Wunused-local-typedefs -Wunused-parameter -Wunused-variable -Wunused-const-variable -Wunused-value -Wunused -Wuse-after-free=3 -Wunknown-pragmas -Wstrict-aliasing -Wstrict-overflow=1 -Wstring-compare -Wstringop-overflow -Wstrict-flex-arrays -Walloc-size -fstrict-flex-arrays -Walloc-zero -Wcalloc-transposed-args -Warith-conversion -Warray-bounds -Warray-compare -Warray-parameter -Wattribute-alias=2 -Wbidi-chars=any -Wbool-compare -Wbool-operation -Wduplicated-branches -Wduplicated-cond -Wframe-address -Wzero-as-null-pointer-constant -Wzero-length-bounds -Wtautological-compare -Wtrampolines -Wfree-nonheap-object -Wpointer-arith -Wtype-limits -Wcomments -Wtrigraphs -Wundef -Wexpansion-to-defined -Wcast-qual -Wcast-align -Wcast-function-type -Wcast-user-defined -Wwrite-strings -Wclobbered -Wconversion -Wdangling-else -Wdangling-pointer -Wempty-body -Wenum-compare -Wenum-conversion -Wsign-compare -Wsign-conversion -Wflex-array-member-not-at-end -Wfloat-conversion -Wsizeof-array-div -Wsizeof-pointer-div -Wsizeof-pointer-memaccess -Wmemset-elt-size -Wmemset-transposed-args -Waddress -Wlogical-op -Wlogical-not-parentheses -Wkeyword-macro -Wheader-guard -Wnormalized=nfc -Wpacked -Wpacked-not-aligned -Wredundant-decls -Wrestrict -Winterference-size -Wint-in-bool-context -Winvalid-utf8 -Wvla -Wvla-parameter -Wvolatile-register-var -pthread -fstack-reuse=all -freg-struct-return -fPIE -fno-plt)
        target_link_options(${ALICE_TEST_NAME} PRIVATE -pthread -flink-libatomic -fuse-ld=wild -static-libgcc -static-libstdc++ -pie)
        if(CMAKE_SYSTEM_NAME STREQUAL "Windows")
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE _UNICODE=)
            target_compile_options(${ALICE_TEST_NAME} PRIVATE -municode)
        endif()
        if(ALICE_SAFE_BUFFERS)
            target_compile_options(${ALICE_TEST_NAME} PRIVATE -fstack-protector-all)
        endif()
        if(ALICE_SAFE_CONTROL_FLOW)
            if(ALICE_SAFE_STACK)
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -fhardened)
            endif()
            target_compile_options(${ALICE_TEST_NAME} PRIVATE -fcf-protection=full -fharden-compares -fharden-conditional-branches -fharden-control-flow-redundancy -fhardcfr-skip-leaf -fhardcfr-check-returning-calls -fhardcfr-check-noreturn-calls=always -fvtable-verify=std)
        endif()
        if(ALICE_SAFE_STACK)
            target_compile_options(${ALICE_TEST_NAME} PRIVATE -fstack-check -fstack-clash-protection)
        endif()
    elseif(CMAKE_CXX_COMPILER_ID STREQUAL "Clang")
        target_compile_options(${ALICE_TEST_NAME} PRIVATE -Werror -Wall -Wextra -Wpedantic -Wconversion -Wsign-conversion -fno-rtti -fno-exceptions -Wno-dollar-in-identifier-extension)
        if(CMAKE_SYSTEM_NAME STREQUAL "Windows")
            target_compile_definitions(${ALICE_TEST_NAME} PRIVATE _UNICODE=)
            target_compile_options(${ALICE_TEST_NAME} PRIVATE -municode)
        endif()
    else()
        target_compile_definitions(${ALICE_TEST_NAME} PRIVATE _HAS_EXCEPTIONS=0)
        if(ALICE_SAFE_BUFFERS)
            target_compile_options(${ALICE_TEST_NAME} PRIVATE /GS)
        endif()
        if(ALICE_SAFE_CONTROL_FLOW)
            target_compile_options(${ALICE_TEST_NAME} PRIVATE /guard:cf)
            target_link_options(${ALICE_TEST_NAME} PRIVATE /guard:cf /CETCOMPAT)
        endif()
        if(ALICE_SAFE_STACK)
            target_compile_options(${ALICE_TEST_NAME} PRIVATE /presetPadding)
        endif()
        if(ALICE_SPECTRE)
            target_compile_options(${ALICE_TEST_NAME} PRIVATE /Qspectre /Qspectre-load /Qspectre-load-cf)
        endif()
        target_compile_options(${ALICE_TEST_NAME} PRIVATE /nologo /bigobj /utf-8 /Zc:preprocessor /W4 /WX /permissive- /GR- /EHs-c- /GA /GF /Gw /Gy /jumptablerdata /options:strict /MP /volatile:iso /Zc:__cplusplus /Zc:enumTypes /Zc:templateScope /Zc:throwingNew)
        target_link_options(${ALICE_TEST_NAME} PRIVATE /NOLOGO /OPT:REF,ICF,LBR /INCREMENTAL:NO /WX)
    endif()
    if(ALICE_MULTI)
        target_compile_definitions(${ALICE_TEST_NAME} PRIVATE $<$<CONFIG:Debug>:alice_debug=>)
        if(MSVC)
            if(ALICE_SAFE_STACK)
                target_compile_options(${ALICE_TEST_NAME} PRIVATE $<$<CONFIG:Debug>:/RTC1>)
            endif()
            target_compile_options(${ALICE_TEST_NAME} PRIVATE $<$<CONFIG:Debug>:/Zi /Od /Zf /ZH:SHA512> $<$<CONFIG:Release>:/GL /Ox /Oi>)
            target_link_options(${ALICE_TEST_NAME} PRIVATE $<$<CONFIG:Debug>:/DEBUG> $<$<CONFIG:Release>:/LTCG /SPGO>)
        else()
            target_compile_options(${ALICE_TEST_NAME} PRIVATE $<$<CONFIG:Debug>:-O0> $<$<CONFIG:Release>:-O3>)
            target_link_options(${ALICE_TEST_NAME} PRIVATE $<$<CONFIG:Debug>:-g>)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                if(ALICE_SAFE_CONTROL_FLOW)
                    target_compile_options(${ALICE_TEST_NAME} PRIVATE $<$<CONFIG:Debug>:-fvtv-debug -fvtv-counts>)
                endif()
                if(ALICE_DEVELOPMENT)
                    target_compile_options(${ALICE_TEST_NAME} PRIVATE $<$<CONFIG:Release>:-fprofile-generate>)
                    target_link_options(${ALICE_TEST_NAME} PRIVATE $<$<CONFIG:Release>:-fprofile-generate>)
                else()
                    target_compile_options(${ALICE_TEST_NAME} PRIVATE $<$<CONFIG:Release>:-fprofile-use -fbranch-probabilities>)
                    target_link_options(${ALICE_TEST_NAME} PRIVATE $<$<CONFIG:Release>:-fprofile-use>)
                endif()
                target_compile_options(${ALICE_TEST_NAME} PRIVATE $<$<CONFIG:Debug>:-g3 -ggdb3 -gdwarf-5 -femit-class-debug-always -fvar-tracking -fvar-tracking-assignments -fvar-tracking-uninit -gsplit-dwarf -gdwarf64 -gdescribe-dies -gpubnames -ggnu-pubnames -fdebug-types-section -grecord-gcc-switches -gas-loc-support -gas-locview-support -gcolumn-info -gstatement-frontiers -gvariable-location-views -ginline-points -gz -funwind-tables -fasynchronous-unwind-tables> $<$<CONFIG:Release>:-flto=auto -fforward-propagate -fomit-frame-pointer -foptimize-crc -foptimize-sibling-calls -foptimize-strlen -finline-atomics -finline-small-functions -findirect-inlining -finline-functions -finline-functions-called-once -fearly-inlining -fipa-sra -fmerge-constants -ffuse-ops-with-volatile-access -fthread-jumps -fsplit-wide-types -fsplit-wide-types-early -fcse-follow-jumps -fcse-skip-blocks -frerun-cse-after-loop -fgcse -fgcse-lm -fgcse-sm -fgcse-las -fgcse-after-reload -faggressive-loop-optimizations -fcrossjumping -fauto-inc-dec -fdce -fdse -fif-conversion -fif-conversion2 -fdeclone-ctor-dtor -fdelete-null-pointer-checks -fdevirtualize -fdevirtualize-speculatively -fdevirtualize-at-ltrans -fexpensive-optimizations -fext-dce -free -flive-range-shrinkage -fira-algorithm=CB -fira-region=all -fira-hoist-pressure -fira-loop-pressure -flra-remat -fschedule-insns -fschedule-insns2 -fsched-pressure -fsched-spec-load -fsched-stalled-insns=0 -fsched-stalled-insns-dep -fsched2-use-superblocks -fsched-group-heuristic -fsched-critical-path-heuristic -fsched-spec-insn-heuristic -fsched-rank-heuristic -fsched-last-insn-heuristic -fsched-dep-count-heuristic -fspeculatively-call-stored-functions -freschedule-modulo-scheduled-loops -fselective-scheduling -fselective-scheduling2 -fsel-sched-pipelining -fsel-sched-pipelining-outer-loops -fshrink-wrap -fshrink-wrap-separate -fcaller-saves -fcombine-stack-adjustments -fipa-ra -ftree-reassoc -fcode-hoisting -ftree-pre -ftree-partial-pre -ftree-forwprop -ftree-fre -ftree-phiprop -fhoist-adjacent-loads -ftree-copy-prop -fipa-pure-const -fipa-reference -fipa-reference-addressable -fipa-reorder-for-locality -fipa-stack-alignment -fipa-pta -fipa-profile -fipa-modref -fipa-cp -fipa-cp-clone -fipa-bit-cp -fipa-vrp -fipa-icf -flate-combine-instructions -fisolate-erroneous-paths-dereference -fisolate-erroneous-paths-attribute -ftree-sink -ftree-bit-ccp -ftree-ccp -fssa-backprop -fssa-phiopt -ftree-switch-conversion -ftree-tail-merge -ftree-cselim -ftree-dce -ftree-builtin-call-dce -ffinite-loops -ftree-dominator-opts -ftree-dse -ftree-ch -ftree-loop-optimize -ftree-loop-linear -floop-strip-mine -floop-block -floop-nest-optimize -ftree-coalesce-vars -ftree-loop-if-convert -ftree-loop-distribution -ftree-loop-distribute-patterns -floop-interchange -floop-unroll-and-jam -ftree-loop-im -ftree-loop-ivcanon -ftree-scev-cprop -fivopts -ftree-pta -ftree-sra -fstore-merging -ftree-ter -ftree-slsr -ftree-vectorize -fvect-cost-model=dynamic -ftree-vrp -fsplit-paths -fsplit-ivs-in-unroller -fpartial-inlining -fpredictive-commoning -freorder-blocks -freorder-blocks-algorithm=stc -freorder-blocks-and-partition -freorder-functions -fstrict-aliasing -fipa-strict-aliasing -falign-functions -flimit-function-alignment -falign-labels -falign-loops -falign-jumps -fweb -fuse-linker-plugin -fcompare-elim -ffold-mem-offsets -fcprop-registers -fprofile-correction -fprofile-partial-training -fauto-profile-inlining -fprofile-values -fprofile-reorder-functions -fvpt -frename-registers -fschedule-fusion -fdep-fusion -ftracer -funroll-loops -fpeel-loops -fmalloc-dce=2 -fmove-loop-invariants -fmove-loop-stores -fsplit-loops -funswitch-loops -fversion-loops-for-strides -ffunction-sections -fdata-sections -fstdarg-opt -fno-ident>)
                target_link_options(${ALICE_TEST_NAME} PRIVATE $<$<CONFIG:Release>:-flto=auto LINKER:--gc-sections>)
            else()
                target_compile_options(${ALICE_TEST_NAME} PRIVATE $<$<CONFIG:Debug>:-g> $<$<CONFIG:Release>:-flto=full>)
                target_link_options(${ALICE_TEST_NAME} PRIVATE $<$<CONFIG:Release>:-flto=full>)
            endif()
        endif()
    elseif(CMAKE_BUILD_TYPE STREQUAL "Debug")
        target_compile_definitions(${ALICE_TEST_NAME} PRIVATE alice_debug=)
        if(MSVC)
            if(ALICE_SAFE_STACK)
                target_compile_options(${ALICE_TEST_NAME} PRIVATE /RTC1)
            endif()
            target_compile_options(${ALICE_TEST_NAME} PRIVATE /Zi /Od /Zf /ZH:SHA512)
            target_link_options(${ALICE_TEST_NAME} PRIVATE /DEBUG)
        else()
            if(ALICE_SAFE_CONTROL_FLOW)
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -fvtv-debug -fvtv-counts)
            endif()
            target_compile_options(${ALICE_TEST_NAME} PRIVATE -O0)
            target_link_options(${ALICE_TEST_NAME} PRIVATE -g)
            if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -g3 -ggdb3 -gdwarf-5 -femit-class-debug-always -fvar-tracking -fvar-tracking-assignments -fvar-tracking-uninit -gsplit-dwarf -gdwarf64 -gdescribe-dies -gpubnames -ggnu-pubnames -fdebug-types-section -grecord-gcc-switches -gas-loc-support -gas-locview-support -gcolumn-info -gstatement-frontiers -gvariable-location-views -ginline-points -gz -funwind-tables -fasynchronous-unwind-tables)
            else()
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -g)
            endif()
        endif()
    elseif(MSVC)
        target_compile_options(${ALICE_TEST_NAME} PRIVATE /GL /Ox /Oi)
        target_link_options(${ALICE_TEST_NAME} PRIVATE /LTCG /SPGO)
    else()
        target_compile_options(${ALICE_TEST_NAME} PRIVATE -O3)
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            if(ALICE_DEVELOPMENT)
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -fprofile-generate)
                target_link_options(${ALICE_TEST_NAME} PRIVATE -fprofile-generate)
            else()
                target_compile_options(${ALICE_TEST_NAME} PRIVATE -fprofile-use -fbranch-probabilities)
                target_link_options(${ALICE_TEST_NAME} PRIVATE -fprofile-use)
            endif()
            target_compile_options(${ALICE_TEST_NAME} PRIVATE -flto=auto -fforward-propagate -fomit-frame-pointer -foptimize-crc -foptimize-sibling-calls -foptimize-strlen -finline-atomics -finline-small-functions -findirect-inlining -finline-functions -finline-functions-called-once -fearly-inlining -fipa-sra -fmerge-constants -ffuse-ops-with-volatile-access -fthread-jumps -fsplit-wide-types -fsplit-wide-types-early -fcse-follow-jumps -fcse-skip-blocks -frerun-cse-after-loop -fgcse -fgcse-lm -fgcse-sm -fgcse-las -fgcse-after-reload -faggressive-loop-optimizations -fcrossjumping -fauto-inc-dec -fdce -fdse -fif-conversion -fif-conversion2 -fdeclone-ctor-dtor -fdelete-null-pointer-checks -fdevirtualize -fdevirtualize-speculatively -fdevirtualize-at-ltrans -fexpensive-optimizations -fext-dce -free -flive-range-shrinkage -fira-algorithm=CB -fira-region=all -fira-hoist-pressure -fira-loop-pressure -flra-remat -fschedule-insns -fschedule-insns2 -fsched-pressure -fsched-spec-load -fsched-stalled-insns=0 -fsched-stalled-insns-dep -fsched2-use-superblocks -fsched-group-heuristic -fsched-critical-path-heuristic -fsched-spec-insn-heuristic -fsched-rank-heuristic -fsched-last-insn-heuristic -fsched-dep-count-heuristic -fspeculatively-call-stored-functions -freschedule-modulo-scheduled-loops -fselective-scheduling -fselective-scheduling2 -fsel-sched-pipelining -fsel-sched-pipelining-outer-loops -fshrink-wrap -fshrink-wrap-separate -fcaller-saves -fcombine-stack-adjustments -fipa-ra -ftree-reassoc -fcode-hoisting -ftree-pre -ftree-partial-pre -ftree-forwprop -ftree-fre -ftree-phiprop -fhoist-adjacent-loads -ftree-copy-prop -fipa-pure-const -fipa-reference -fipa-reference-addressable -fipa-reorder-for-locality -fipa-stack-alignment -fipa-pta -fipa-profile -fipa-modref -fipa-cp -fipa-cp-clone -fipa-bit-cp -fipa-vrp -fipa-icf -flate-combine-instructions -fisolate-erroneous-paths-dereference -fisolate-erroneous-paths-attribute -ftree-sink -ftree-bit-ccp -ftree-ccp -fssa-backprop -fssa-phiopt -ftree-switch-conversion -ftree-tail-merge -ftree-cselim -ftree-dce -ftree-builtin-call-dce -ffinite-loops -ftree-dominator-opts -ftree-dse -ftree-ch -ftree-loop-optimize -ftree-loop-linear -floop-strip-mine -floop-block -floop-nest-optimize -ftree-coalesce-vars -ftree-loop-if-convert -ftree-loop-distribution -ftree-loop-distribute-patterns -floop-interchange -floop-unroll-and-jam -ftree-loop-im -ftree-loop-ivcanon -ftree-scev-cprop -fivopts -ftree-pta -ftree-sra -fstore-merging -ftree-ter -ftree-slsr -ftree-vectorize -fvect-cost-model=dynamic -ftree-vrp -fsplit-paths -fsplit-ivs-in-unroller -fpartial-inlining -fpredictive-commoning -freorder-blocks -freorder-blocks-algorithm=stc -freorder-blocks-and-partition -freorder-functions -fstrict-aliasing -fipa-strict-aliasing -falign-functions -flimit-function-alignment -falign-labels -falign-loops -falign-jumps -fweb -fuse-linker-plugin -fcompare-elim -ffold-mem-offsets -fcprop-registers -fprofile-correction -fprofile-partial-training -fauto-profile-inlining -fprofile-values -fprofile-reorder-functions -fvpt -frename-registers -fschedule-fusion -fdep-fusion -ftracer -funroll-loops -fpeel-loops -fmalloc-dce=2 -fmove-loop-invariants -fmove-loop-stores -fsplit-loops -funswitch-loops -fversion-loops-for-strides -ffunction-sections -fdata-sections -fstdarg-opt -fno-ident)
            target_link_options(${ALICE_TEST_NAME} PRIVATE -flto=auto LINKER:--gc-sections)
        else()
            target_compile_options(${ALICE_TEST_NAME} PRIVATE -flto=full)
            target_link_options(${ALICE_TEST_NAME} PRIVATE -flto=full)
        endif()
    endif()
    add_test(NAME ${ALICE_TEST_FILE} COMMAND ${ALICE_TEST_NAME})
    if(CMAKE_SYSTEM_NAME STREQUAL "Linux")
        if(ALICE_CI)
            set_tests_properties(${ALICE_TEST_FILE} PROPERTIES ENVIRONMENT "VK_LAYER_PATH=${ALICE_VK_LAYER_PATH};WAYLAND_DISPLAY=wayland-0")
        else()
            set_tests_properties(${ALICE_TEST_FILE} PROPERTIES ENVIRONMENT "VK_LAYER_PATH=${ALICE_VK_LAYER_PATH}")
        endif()
    endif()
    list(APPEND ALICE_TESTING ${ALICE_TEST_NAME})
endforeach()
add_custom_target(clean_app ${ALICE_CLEAN})
add_custom_target(ALICE_RUN_TESTS ALL COMMAND ${CMAKE_CTEST_COMMAND} --output-on-failure WORKING_DIRECTORY ${CMAKE_BINARY_DIR})
add_dependencies(ALICE_RUN_TESTS ${ALICE_TESTING})
execute_process(COMMAND ${CMAKE_COMMAND} -E environment OUTPUT_FILE "${CMAKE_CURRENT_SOURCE_DIR}/Environment")